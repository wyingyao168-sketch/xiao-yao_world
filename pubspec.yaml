name: xiaoyao_world
description: A serene app for Mini Jie.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
  assets:
    - app001.jpg  # 这行最关键，它告诉代码去哪里找你的星空图
